逝爱 - 启动说明
===================

最简单的方式（推荐）：
1. 双击"启动后端.bat"
2. 双击"启动前端.bat"
3. 打开浏览器访问 http://localhost:5173

---

首次使用需要安装依赖：

1. 安装前端依赖：
   打开命令行，运行：npm install

2. 安装后端依赖：
   打开命令行，运行：
   cd backend
   pip install -r requirements.txt

---

如果不想用虚拟环境，直接运行Python即可！
